Here I put what should I do on my code:

To fetch a sub-array

I must have at least one condition to slice the array. Must be something like from the index a to the index b or something related, like left-half and right-half. This exercise is similar to the previous one where I did show numbers within the array only under certain conditions, but in this one I will create a new array to keep the information. That could be a great exercise, stimulating different areas, such as memory allocation and array copy.

So...

Condition 1
	Save numbers from a specific index to another.
	-To achieve this, I must have two variables, index1 and index2 to keep the indexes.
	-calculate the difference between these two indexes. This will be the size of my new 
	array "fetched". Now I can locate memory for this new array
	- Now I can copy the specific numbers into the new array


To rotate the array

To be able to rotate the array, I must guarantee that all the array has been rotated. 
I can do that by inserting a counter thae keeps the number of rotations made. The counter must be the size of the array. Once this number is reached it means that all the array has been rotated.

Condition 1 - rotation to the left.
It means that the number should ocupy the position on it's left, what means a smaller index.
I can do that by reducing the index by the "displacement" value passed as parameter. 
	- The problem that I must consider is when the index reduction reachs zero but I still got rotations to do, so, the operation must analyze if there is rotations to do and
	if the counter didn't reach the size of the array.

Condition 2 - rotation to the right.
It is pretty similar to the previous condition, changing only the direction.
in this case, the problem is when the last index is reached, not the first.